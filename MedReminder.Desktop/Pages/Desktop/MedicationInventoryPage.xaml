<?xml version="1.0" encoding="utf-8" ?>
<pages:AuthPage
    x:Name="Root"
    x:Class="MedReminder.Pages.Desktop.MedicationInventoryPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:MedReminder.Pages"
    Title="Medication Inventory"
    BackgroundColor="{StaticResource Medication_Main_Background}">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Refresh" Clicked="OnRefreshClicked" Priority="0" Order="Primary" />
        <ToolbarItem Text="Logout" Clicked="OnLogoutClicked" Priority="1" Order="Primary" />
    </ContentPage.ToolbarItems>

    <Grid Padding="16" RowDefinitions="Auto,*" RowSpacing="12">

        <!-- Top bar: Search + action Button (match ResidentsPage layout) -->
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">

            <SearchBar x:Name="MedSearchBar"
                       Placeholder="Search medication..."
                       TextChanged="OnFilterChanged" />

            <HorizontalStackLayout Grid.Column="1" Spacing="8">

                <!-- Sort -->
                <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="4"/>
                    </Border.StrokeShape>

                    <Grid ColumnDefinitions="32,Auto" Margin="0">
                        <Image Grid.Column="0"
                               Source="icons8_sort_100.png"
                               HeightRequest="16"
                               WidthRequest="16"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>

                        <Border Grid.Column="1"
                                BackgroundColor="{StaticResource Button_View}"
                                Padding="10,0"
                                StrokeThickness="0">
                            <Label Text=" SORT "
                                   TextColor="{StaticResource Button_Font_Primary}"
                                   FontSize="12"
                                   VerticalOptions="Center"
                                   FontAttributes="Bold"/>
                        </Border>
                    </Grid>

                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnSortClicked" />
                    </Border.GestureRecognizers>
                </Border>

                <!-- Orders -->
                <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="4"/>
                    </Border.StrokeShape>

                    <Grid ColumnDefinitions="32,Auto" Margin="0">
                        <Image Grid.Column="0" Source="icons8_basketadd_100.png"
                               HeightRequest="16" WidthRequest="16"
                               HorizontalOptions="Center" VerticalOptions="Center"/>

                        <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Create}"
                                Padding="10,0" StrokeThickness="0">
                            <Label Text=" ORDERS "
                                   TextColor="{StaticResource Button_Font_Primary}" FontSize="12"
                                   VerticalOptions="Center" FontAttributes="Bold"/>
                        </Border>
                    </Grid>

                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnOrdersClicked" />
                    </Border.GestureRecognizers>
                </Border>

            </HorizontalStackLayout>
        </Grid>

        <!-- Inventory list -->
        <CollectionView x:Name="InventoryCollection" Grid.Row="1" ItemsSource="{Binding Items}">

            <CollectionView.EmptyView>
                <Label Text="No inventory items found." 
                       Opacity="0.6" VerticalOptions="Center" />
            </CollectionView.EmptyView>

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="6"/>
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12"
                            BackgroundColor="{StaticResource Medication_Background}"
                            Stroke="{StaticResource Medication_Font_Primary}"
                            StrokeThickness="1">

                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8"/>
                        </Border.StrokeShape>

                        <Grid ColumnDefinitions="*,Auto"
                              ColumnSpacing="10"
                              RowSpacing="8">

                            <!-- Row 0 -->
                            <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                                <Label Text="{Binding MedName}" FontSize="18" FontAttributes="Bold" TextColor="{StaticResource Medication_Font_Primary}" />
                                <BoxView WidthRequest="1" HeightRequest="20" Color="{StaticResource Medication_Font_Secondary}" VerticalOptions="Center"/>

                                <Label Text="{Binding Usage}" FontSize="13" TextColor="{StaticResource Medication_Font_Secondary}" VerticalOptions="Center"/>
                                <BoxView WidthRequest="1" HeightRequest="20" Color="{StaticResource Medication_Font_Secondary}" VerticalOptions="Center"/>

                                <Label FontSize="13" TextColor="{StaticResource Medication_Font_Secondary}" VerticalOptions="Center">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Stock: "/>
                                            <Span Text="{Binding StockQuantity}"/>
                                            <Span Text=" • "/>
                                            <Span Text="Reorder: "/>
                                            <Span Text="{Binding ReorderLevel}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </HorizontalStackLayout>

                            <!-- Right side: Low Stock + Adjust + Order -->
                            <HorizontalStackLayout Grid.Column="1" Spacing="8" HorizontalOptions="End" VerticalOptions="Center">

                                <!-- Low stock badge -->

                                <Border Padding="8,4" BackgroundColor="{StaticResource Alert_Warning}" IsVisible="{Binding IsLowStock}" StrokeThickness="0">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="6"/>
                                    </Border.StrokeShape>
                                    <Label Text="LOW" TextColor="{StaticResource Button_Font_Primary}"  FontAttributes="Bold" FontSize="12" />
                                </Border>

                                <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="4"/>
                                    </Border.StrokeShape>
                                    <Grid ColumnDefinitions="32,Auto" Margin="0">
                                        <Image Grid.Column="0" Source="icons8_edit_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                                        <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Edit}" Padding="10,0" StrokeThickness="0">
                                            <Label Text="ADJUST" TextColor="{StaticResource Button_Font_Primary}" FontSize="12" VerticalOptions="Center" FontAttributes="Bold"/>
                                        </Border>
                                    </Grid>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnAdjustClicked" CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>
                                </Border>

                                <!-- Edit -->
                                <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="4"/>
                                    </Border.StrokeShape>
                                    <Grid ColumnDefinitions="32,Auto" Margin="0">
                                        <Image Grid.Column="0" Source="icons8_basketadd_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                                        <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Create}" Padding="10,0" StrokeThickness="0">
                                            <Label Text="ORDERS" TextColor="{StaticResource Button_Font_Primary}" FontSize="12" VerticalOptions="Center" FontAttributes="Bold"/>
                                        </Border>
                                    </Grid>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnDirectOrderClicked" CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>
                                </Border>
                            </HorizontalStackLayout>

                        </Grid>

                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </Grid>
</pages:AuthPage>
