<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MedReminder.Pages.Desktop.ResidentObservationsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Observations"
    BackgroundColor="{StaticResource Observation_Main_Background}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Logout" Clicked="OnLogoutClicked" Priority="1" Order="Primary" />
    </ContentPage.ToolbarItems>

    <Grid Padding="16" RowDefinitions="Auto,Auto,Auto,*" RowSpacing="12">

        <!-- ── Page header ─────────────────────────────────────────────────── -->
        <Grid ColumnDefinitions="*,Auto">
            <VerticalStackLayout Spacing="2">
                <Label Text="RESIDENT OBSERVATIONS"
                       FontSize="11" FontAttributes="Bold" CharacterSpacing="1.5"
                       TextColor="{StaticResource Observation_Font_Secondary}"/>
                <Label Text="{Binding ResidentName}"
                       FontSize="20" FontAttributes="Bold"
                       TextColor="{StaticResource Observation_Font_Primary}"/>
            </VerticalStackLayout>

            <Border Grid.Column="1"
                    Padding="8,4" StrokeThickness="0"
                    BackgroundColor="{StaticResource Alert_Warning}"
                    VerticalOptions="Center"
                    IsVisible="{Binding IsOffline}">
                <Border.StrokeShape><RoundRectangle CornerRadius="4"/></Border.StrokeShape>
                <Label Text="OFFLINE" FontSize="10" FontAttributes="Bold"
                       TextColor="{StaticResource Button_Font_Primary}"/>
            </Border>
        </Grid>

        <!-- ── Top action bar ──────────────────────────────────────────────── -->
        <HorizontalStackLayout Grid.Row="1" Spacing="8">

            <!-- ADD — dimmed when not available -->
            <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}"
                    InputTransparent="{Binding CanAdd, Converter={StaticResource InverseBoolConverter}}">
                <Border.StrokeShape><RoundRectangle CornerRadius="4"/></Border.StrokeShape>
                <Border.Triggers>
                    <DataTrigger TargetType="Border" Binding="{Binding CanAdd}" Value="False">
                        <Setter Property="Opacity" Value="0.35"/>
                    </DataTrigger>
                </Border.Triggers>
                <Grid ColumnDefinitions="32,Auto" Margin="0" InputTransparent="True">
                    <Image Grid.Column="0" Source="icons8_add_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                    <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Add}" Padding="10,0" StrokeThickness="0" InputTransparent="True">
                        <Label Text="ADD" TextColor="{StaticResource Button_Font_Primary}" FontSize="11" VerticalOptions="Center" FontAttributes="Bold"/>
                    </Border>
                </Grid>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding AddObservationCommand}"/>
                </Border.GestureRecognizers>
            </Border>

            <!-- EDIT — dimmed when no row selected -->
            <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}"
                    InputTransparent="{Binding CanEdit, Converter={StaticResource InverseBoolConverter}}">
                <Border.StrokeShape><RoundRectangle CornerRadius="4"/></Border.StrokeShape>
                <Border.Triggers>
                    <DataTrigger TargetType="Border" Binding="{Binding CanEdit}" Value="False">
                        <Setter Property="Opacity" Value="0.35"/>
                    </DataTrigger>
                </Border.Triggers>
                <Grid ColumnDefinitions="32,Auto" Margin="0" InputTransparent="True">
                    <Image Grid.Column="0" Source="icons8_edit_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                    <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Edit}" Padding="10,0" StrokeThickness="0" InputTransparent="True">
                        <Label Text="EDIT" TextColor="{StaticResource Button_Font_Secondary}" FontSize="11" VerticalOptions="Center" FontAttributes="Bold"/>
                    </Border>
                </Grid>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding EditCommand}" CommandParameter="{Binding SelectedObservation}"/>
                </Border.GestureRecognizers>
            </Border>

            <!-- REFRESH -->
            <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}">
                <Border.StrokeShape><RoundRectangle CornerRadius="4"/></Border.StrokeShape>
                <Grid ColumnDefinitions="32,Auto" Margin="0" InputTransparent="True">
                    <Image Grid.Column="0" Source="icons8_updates_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                    <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Logout}" Padding="10,0" StrokeThickness="0" InputTransparent="True">
                        <Label Text="REFRESH" TextColor="{StaticResource Button_Font_Primary}" FontSize="11" VerticalOptions="Center" FontAttributes="Bold"/>
                    </Border>
                </Grid>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding RefreshCommand}"/>
                </Border.GestureRecognizers>
            </Border>

            <!-- SYNC — visible only when offline -->
            <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}"
                    IsVisible="{Binding IsOffline}">
                <Border.StrokeShape><RoundRectangle CornerRadius="4"/></Border.StrokeShape>
                <Grid ColumnDefinitions="32,Auto" Margin="0" InputTransparent="True">
                    <Image Grid.Column="0" Source="icons8_updates_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                    <Border Grid.Column="1" BackgroundColor="{StaticResource Alert_Info}" Padding="10,0" StrokeThickness="0" InputTransparent="True">
                        <Label Text="SYNC" TextColor="{StaticResource Button_Font_Primary}" FontSize="11" VerticalOptions="Center" FontAttributes="Bold"/>
                    </Border>
                </Grid>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding SyncCommand}"/>
                </Border.GestureRecognizers>
            </Border>

            <!-- CLOSE — back to resident page -->
            <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}">
                <Border.StrokeShape><RoundRectangle CornerRadius="4"/></Border.StrokeShape>
                <Grid ColumnDefinitions="32,Auto" Margin="0" InputTransparent="True">
                    <Image Grid.Column="0" Source="icons8_close_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                    <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Close}" Padding="10,0" StrokeThickness="0" InputTransparent="True">
                        <Label Text="CLOSE" TextColor="{StaticResource Button_Font_Primary}" FontSize="11" VerticalOptions="Center" FontAttributes="Bold"/>
                    </Border>
                </Grid>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnCloseClicked"/>
                </Border.GestureRecognizers>
            </Border>

        </HorizontalStackLayout>

        <!-- ── Status + Date range filter ─────────────────────────────────── -->
        <Grid Grid.Row="2" ColumnDefinitions="*,Auto" ColumnSpacing="8">

            <Label Text="{Binding StatusMessage}"
                   VerticalOptions="Center" FontSize="12"
                   TextColor="{StaticResource Observation_Font_Secondary}"/>

            <!-- TODAY / 3 DAYS / 7 DAYS toggle -->
            <Grid Grid.Column="1" ColumnDefinitions="*,*,*" WidthRequest="210">

                <Border Grid.Column="0" HeightRequest="28" Padding="0" StrokeThickness="0"
                        BackgroundColor="{StaticResource Button_Cancel}">
                    <Border.StrokeShape><RoundRectangle CornerRadius="4,0,4,0"/></Border.StrokeShape>
                    <Border.Triggers>
                        <DataTrigger TargetType="Border" Binding="{Binding IsRangeToday}" Value="True">
                            <Setter Property="BackgroundColor" Value="{StaticResource Alert_Info}"/>
                        </DataTrigger>
                    </Border.Triggers>
                    <Label Text="TODAY" FontSize="10" FontAttributes="Bold"
                           TextColor="{StaticResource Button_Font_Primary}"
                           VerticalOptions="Center" HorizontalOptions="Center"/>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding TodayCommand}"/>
                    </Border.GestureRecognizers>
                </Border>

                <Border Grid.Column="1" HeightRequest="28" Padding="0" StrokeThickness="0"
                        BackgroundColor="{StaticResource Button_Cancel}">
                    <Border.StrokeShape><RoundRectangle CornerRadius="0"/></Border.StrokeShape>
                    <Border.Triggers>
                        <DataTrigger TargetType="Border" Binding="{Binding IsRange3Days}" Value="True">
                            <Setter Property="BackgroundColor" Value="{StaticResource Alert_Info}"/>
                        </DataTrigger>
                    </Border.Triggers>
                    <Label Text="3 DAYS" FontSize="10" FontAttributes="Bold"
                           TextColor="{StaticResource Button_Font_Primary}"
                           VerticalOptions="Center" HorizontalOptions="Center"/>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Last3Command}"/>
                    </Border.GestureRecognizers>
                </Border>

                <Border Grid.Column="2" HeightRequest="28" Padding="0" StrokeThickness="0"
                        BackgroundColor="{StaticResource Button_Cancel}">
                    <Border.StrokeShape><RoundRectangle CornerRadius="0,4,0,4"/></Border.StrokeShape>
                    <Border.Triggers>
                        <DataTrigger TargetType="Border" Binding="{Binding IsRange7Days}" Value="True">
                            <Setter Property="BackgroundColor" Value="{StaticResource Alert_Info}"/>
                        </DataTrigger>
                    </Border.Triggers>
                    <Label Text="7 DAYS" FontSize="10" FontAttributes="Bold"
                           TextColor="{StaticResource Button_Font_Primary}"
                           VerticalOptions="Center" HorizontalOptions="Center"/>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Last7Command}"/>
                    </Border.GestureRecognizers>
                </Border>

            </Grid>
        </Grid>

        <!-- ── Main content card ───────────────────────────────────────────── -->
        <Border Grid.Row="3"
                Padding="12,10"
                BackgroundColor="{StaticResource Observation_Card_Background}"
                Stroke="{StaticResource Observation_Card_Border}"
                StrokeThickness="1">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="12"/>
            </Border.StrokeShape>

            <Grid RowDefinitions="Auto,Auto,*">

                <Label Text="RECORDS"
                       FontSize="11" FontAttributes="Bold" CharacterSpacing="1.5"
                       TextColor="{StaticResource Observation_Font_Secondary}"/>

                <BoxView Grid.Row="1" HeightRequest="1" Color="{StaticResource Observation_Card_Border}" Margin="0,6,0,10"/>

                <RefreshView Grid.Row="2"
                             IsRefreshing="{Binding IsRefreshing}"
                             Command="{Binding RefreshCommand}">
                    <CollectionView ItemsSource="{Binding Observations}"
                                   SelectionMode="Single"
                                   SelectedItem="{Binding SelectedObservation}">

                        <CollectionView.EmptyView>
                            <VerticalStackLayout Padding="20" Spacing="6"
                                                 VerticalOptions="Center" HorizontalOptions="Center">
                                <Label Text="No observations recorded."
                                       FontAttributes="Bold" FontSize="14"
                                       TextColor="{StaticResource Observation_Font_Primary}"
                                       HorizontalOptions="Center"/>
                                <Label Text="Use ADD to record a new note or vital."
                                       FontSize="12" TextColor="{StaticResource Observation_Font_Secondary}"
                                       HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </CollectionView.EmptyView>

                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical" ItemSpacing="8"/>
                        </CollectionView.ItemsLayout>

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,10"
                                        BackgroundColor="{StaticResource Observation_Main_Background}"
                                        Stroke="{StaticResource Observation_Card_Border}"
                                        StrokeThickness="1">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="8"/>
                                    </Border.StrokeShape>

                                    <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="5">

                                        <!-- Type badge + date + actions -->
                                        <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="6">

                                            <Border Padding="6,2" StrokeThickness="0"
                                                    BackgroundColor="{StaticResource Observation_Card_Border}"
                                                    VerticalOptions="Center">
                                                <Border.StrokeShape><RoundRectangle CornerRadius="3"/></Border.StrokeShape>
                                                <Label Text="{Binding Type}"
                                                       FontSize="10" FontAttributes="Bold"
                                                       TextColor="{StaticResource Button_Font_Primary}"/>
                                            </Border>

                                            <Label Grid.Column="1"
                                                   Text="{Binding RecordedAt, Converter={StaticResource UtcToLocalDateTimeConverter}}"
                                                   FontSize="11"
                                                   TextColor="{StaticResource Observation_Font_Secondary}"
                                                   VerticalOptions="Center"/>

                                            <!-- EDIT -->
                                            <Border Grid.Column="2" StrokeThickness="0" HeightRequest="24"
                                                    BackgroundColor="{StaticResource Button_Icon_Background}"
                                                    VerticalOptions="Center">
                                                <Border.StrokeShape><RoundRectangle CornerRadius="3"/></Border.StrokeShape>
                                                <Grid ColumnDefinitions="22,Auto" Margin="0" InputTransparent="True">
                                                    <Image Grid.Column="0" Source="icons8_edit_100.png" HeightRequest="12" WidthRequest="12" HorizontalOptions="Center" VerticalOptions="Center"/>
                                                    <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Edit}" Padding="7,0" StrokeThickness="0" InputTransparent="True">
                                                        <Label Text="EDIT" TextColor="{StaticResource Button_Font_Secondary}" FontSize="10" VerticalOptions="Center" FontAttributes="Bold"/>
                                                    </Border>
                                                </Grid>
                                                <Border.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type CollectionView}}, Path=BindingContext.EditCommand}"
                                                                          CommandParameter="{Binding .}"/>
                                                </Border.GestureRecognizers>
                                            </Border>

                                            <!-- DELETE -->
                                            <Border Grid.Column="3" StrokeThickness="0" HeightRequest="24"
                                                    BackgroundColor="{StaticResource Button_Icon_Background}"
                                                    VerticalOptions="Center">
                                                <Border.StrokeShape><RoundRectangle CornerRadius="3"/></Border.StrokeShape>
                                                <Grid ColumnDefinitions="22,Auto" Margin="0" InputTransparent="True">
                                                    <Image Grid.Column="0" Source="icons8_delete_100.png" HeightRequest="12" WidthRequest="12" HorizontalOptions="Center" VerticalOptions="Center"/>
                                                    <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Delete}" Padding="7,0" StrokeThickness="0" InputTransparent="True">
                                                        <Label Text="DELETE" TextColor="{StaticResource Button_Font_Primary}" FontSize="10" VerticalOptions="Center" FontAttributes="Bold"/>
                                                    </Border>
                                                </Grid>
                                                <Border.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type CollectionView}}, Path=BindingContext.DeleteCommand}"
                                                                          CommandParameter="{Binding .}"/>
                                                </Border.GestureRecognizers>
                                            </Border>

                                        </Grid>

                                        <!-- Value -->
                                        <Label Grid.Row="1"
                                               Text="{Binding Value}"
                                               FontSize="13"
                                               TextColor="{StaticResource Observation_Font_Primary}"
                                               LineBreakMode="WordWrap"/>

                                        <!-- Recorded by -->
                                        <Label Grid.Row="2"
                                               Text="{Binding RecordedBy, StringFormat='By: {0}'}"
                                               FontSize="11"
                                               TextColor="{StaticResource Observation_Font_Secondary}"/>

                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>

                    </CollectionView>
                </RefreshView>

            </Grid>
        </Border>

    </Grid>
</ContentPage>
