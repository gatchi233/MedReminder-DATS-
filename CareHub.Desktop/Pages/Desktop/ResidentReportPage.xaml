<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="CareHub.Pages.Desktop.ResidentReportPage"
    Title="Resident Report"
    BackgroundColor="{StaticResource Resident_Main_Background}">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">

            <!-- Top action buttons -->
            <HorizontalStackLayout Spacing="8">

                <!-- Share Report -->
                <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="4"/>
                    </Border.StrokeShape>
                    <Grid ColumnDefinitions="32,Auto" Margin="0" InputTransparent="True">
                        <Image Grid.Column="0" Source="icons8_report_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                        <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Export}" Padding="10,0" StrokeThickness="0" InputTransparent="True">
                            <Label Text="SHARE REPORT" TextColor="{StaticResource Button_Font_Primary}" FontSize="11" VerticalOptions="Center" FontAttributes="Bold"/>
                        </Border>
                    </Grid>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ShareReportCommand}"/>
                    </Border.GestureRecognizers>
                </Border>

                <!-- AI DRAFT -->
                <Border x:Name="AiDraftAction" StrokeThickness="0" HeightRequest="28"
                        BackgroundColor="{StaticResource Button_Icon_Background}">
                    <Border.StrokeShape><RoundRectangle CornerRadius="4"/></Border.StrokeShape>
                    <Grid ColumnDefinitions="32,Auto" Margin="0" InputTransparent="True">
                        <Label Grid.Column="0" Text="*" FontSize="16" FontAttributes="Bold"
                               TextColor="#8B5CF6"
                               HorizontalOptions="Center" VerticalOptions="Center"/>
                        <Border Grid.Column="1" BackgroundColor="#7C3AED"
                                Padding="10,0" StrokeThickness="0" InputTransparent="True">
                            <Label Text="AI DRAFT" TextColor="White"
                                   FontSize="11" VerticalOptions="Center" FontAttributes="Bold"/>
                        </Border>
                    </Grid>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnAiDraftClicked"/>
                    </Border.GestureRecognizers>
                </Border>

                <!-- Close -->
                <Border StrokeThickness="0" HeightRequest="28" BackgroundColor="{StaticResource Button_Icon_Background}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="4"/>
                    </Border.StrokeShape>
                    <Grid ColumnDefinitions="32,Auto" Margin="0" InputTransparent="True">
                        <Image Grid.Column="0" Source="icons8_close_100.png" HeightRequest="16" WidthRequest="16" HorizontalOptions="Center" VerticalOptions="Center"/>
                        <Border Grid.Column="1" BackgroundColor="{StaticResource Button_Close}" Padding="10,0" StrokeThickness="0" InputTransparent="True">
                            <Label Text="CLOSE" TextColor="{StaticResource Button_Font_Primary}" FontSize="11" VerticalOptions="Center" FontAttributes="Bold"/>
                        </Border>
                    </Grid>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnCloseClicked"/>
                    </Border.GestureRecognizers>
                </Border>

            </HorizontalStackLayout>

            <Label Text="{Binding TitleText}"
                   FontSize="22"
                   FontAttributes="Bold"
                   TextColor="{StaticResource Resident_Font_Primary}"/>

            <Label Text="{Binding SubtitleText}"
                   FontSize="12"
                   TextColor="{StaticResource Resident_Font_Secondary}"/>

            <Label Text="Preview (HTML):"
                   FontAttributes="Bold"
                   TextColor="{StaticResource Resident_Font_Primary}"/>

            <Editor Text="{Binding HtmlPreview}"
                    AutoSize="TextChanges"
                    IsReadOnly="True"
                    FontFamily="Consolas"
                    TextColor="{StaticResource Resident_Font_Primary}"/>

            <!-- AI Draft Section -->
            <Border x:Name="AiDraftSection" Padding="12,10" Margin="0,8,0,0"
                    BackgroundColor="#F5F3FF"
                    Stroke="#7C3AED" StrokeThickness="1"
                    IsVisible="False">
                <Border.StrokeShape><RoundRectangle CornerRadius="8"/></Border.StrokeShape>
                <VerticalStackLayout Spacing="6">
                    <Label Text="AI-GENERATED DRAFT — REVIEW REQUIRED"
                           FontSize="10" FontAttributes="Bold" CharacterSpacing="1"
                           TextColor="#7C3AED"/>
                    <BoxView HeightRequest="1" Color="#7C3AED" Opacity="0.3"/>
                    <Label x:Name="AiDraftLabel" Text=""
                           FontSize="13" LineBreakMode="WordWrap"
                           TextColor="{StaticResource Resident_Font_Primary}"/>
                    <Label FontSize="10" FontAttributes="Italic"
                           TextColor="#92400E"
                           Text="This draft is AI-generated and must be reviewed by qualified staff before use."/>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
