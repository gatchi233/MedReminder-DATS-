<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MedReminder.Mobile.Pages.ResidentsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Residents">

    <CollectionView ItemsSource="{Binding Residents}"
                    SelectionMode="Single"
                    SelectionChanged="OnSelectionChanged">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <VerticalStackLayout Padding="14" Spacing="4">
                    <!-- Adjust property names if needed -->
                    <Label Text="{Binding ResidentName}" FontSize="18" />
                    <Label Text="{Binding Remarks}" FontSize="12" Opacity="0.6" />
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
