# MedReminder Project Status

## Completed (Current Working / Implemented)

### Core App Structure

- Desktop + Mobile solution split (`MedReminder.Desktop` / `MedReminder.Mobile`)
- Models + Services architecture in place (Interfaces → Local JSON Services)
- Local JSON data files in Desktop (`Residents.json`, `Medications.json`, `Observations.json, Inventory.json`)
- Dependency Injection (DI) wired in `MauiProgram.cs`

### Desktop Pages & Navigation

- `LoginPage` exists and works for demo (staff login via `AuthService`)
- Desktop pages exist and are connected by ViewModels:
  - `HomePage`
  - `ResidentsPage`
  - `ViewResidentPage`
  - `EditResidentPage`
  - `ResidentMedicationsPage`
  - `EditMedicationPage`
  - `ResidentReportPage`
  - `MedicationOrdersPage`
  - `MedicationInventoryPage`
  - `FloorPlanPage`
- Basic navigation through Shell routes is working (need to check again)

### Reporting (Demo Feature)

- `ResidentReportBuilder` + `ResidentReportViewModel` implemented
- Report can be generated and displayed (UX polish still pending)

### Mobile

- Mobile residents list page exists (`ResidentsPage`)
- Mobile resident detail page exists (`ResidentDetailPage`)

---

## To Do List

### A. Mobile Module

- Verify app launches
- Verify residents list loads
- Verify resident detail page loads
- Fix target frameworks / Shell routing / JSON loading if broken

---

### B. Floor Plan

- Build final floor plan layout:
  - 1 page only, with separator line for 1/F and 2/F
  - 12 rooms per floor (7 single + 5 double)
  - Clicking resident card navigates to `ViewResidentPage`

---

### C. Ordering / Inventory

#### Inventory Screen

- Medication stock list
- Low stock indicators
- Reorder threshold (even simple)

#### Orders Workflow

- Create medication order (qty, med, date, staff name)
- Status flow: Requested → Ordered → Received
- Order history list

---

### D. Staff Management Page

- Add Staff Management Page (Admin-only)
- CRUD-ish features (simple, demo-friendly):
  - Add staff (name, username, role)
  - Enable/Disable staff
  - Role label (Admin/Staff)
- Store staff list in JSON (`Staff.json`)

---

### E. UI/UX Polish

- Proper close/back behavior on report page
- Disable back navigation after logout
- Default desktop window to ~75% screen size (still resizable)

---

### F. Database Upgrade

- ASP.NET Core Web API
- PostgreSQL schema + migrations
- Replace local JSON services with API calls
- Multi-user demo: run on 2 machines, shared DB

---

### G. AI Assistant Feature

- AI assistant panel (Desktop)
- Safe prompts with resident context
- Example:
  - "Who is overdue today?"
  - "Summarize today's meds"
  - "Flag missing dosage / duplicates / expired soon"

---

## Documentation Deliverables

### 1. TDD

Minimum sections to submit:

- Project overview + objectives
- User roles + user stories
- System architecture (Desktop/Mobile + services)
- Data design (JSON now, API/DB later)
- Navigation & page descriptions
- Ordering workflow spec
- Error handling + validation rules
- Testing plan + demo plan

### 2. Process Flow Diagrams

Include at least:

- Desktop navigation flow (Login → Home → Residents → View → Medications → Edit)
- Order workflow flow (Create → Ordered → Received)
- Optional: AI query flow

### 3. Wireframes

Minimum set:

- Login
- Home
- Residents list
- Resident detail
- Resident medications list
- Edit medication
- Inventory
- Orders
- Floor plan layout
- Staff management page
